<!-- judge-r114(code-reviewer,opus,新开)盲判报告;裁判环境禁写 .md,由主会话从其最终回复原样落盘 2026-09-07 -->

# ACCEPT-REPORT r114 —— 独立验收裁定

判据来源仅限四个文件：`BRIEF-r114.md`、`TEST-PLAN-r114.md`、`test-output-r114-full.txt`、`test-output-r114-realmachine.txt`。未读源码、未跑测试、未查 git。

## 总结论

**合格，但有待真机项** —— 自动化这条腿全绿且证据自洽；真机这条腿只跑了不花钱的部分（M9/M13/M10 一半），**BRIEF 明确点名"必须真机分开验证"的停止语义（M1–M3）与实时分阶段视图（M6）目前零真机证据**，不能算完成。

按 TEST-PLAN 逐行统计（共 104 行）：

| 判定 | 条数 |
|---|---|
| 过 | 93 |
| 部分过 | 1（M10） |
| 未验证 | 10（M1–M8、M11、M12） |
| 不过 | 0 |

---

## 一、四项特别核对（先说要命的）

**① HEAD 一致性：一致。**
- 全量输出头：`worktree HEAD f8f7af5c,锁定 3edf6c39`
- 真机记录头：`worktree HEAD f8f7af5c(修前对照 = HEAD bac94646)`
- 当前被测 HEAD 两边都是 `f8f7af5c`，**通过**。另两个哈希角色不同（`3edf6c39` 是源码锁的比对基线，`bac94646` 是真机"修前"对照构建），不构成矛盾，但见风险 R1。

**② 退出码：388 个测试文件全部 `exit=0`，无一非零；末尾 `client eslint (npx eslint src)` 也是 `exit=0`。** 全文无 `✗`、无 `失败 N(N>0)`、无 `红 N(N>0)`。通过。

**③ 红转绿 / 绿保绿：全部达标，且计数能对上账。**

| 文件 | 汇总 | 与 TEST-PLAN 对账 |
|---|---|---|
| `check-r114-server` | 120 绿 / 0 红（修前应红 114 + 修前应绿 6） | §二里属服务端的回归项正好 6 条（A3-2、A2-18、A2-19、A4-0、A4-5、A4-6）✅ |
| `check-r114-client` | 97 绿 / 0 红（95 + 2） | §二里属前端的回归项正好 2 条（B1-3、B1-8）✅ |
| `check-r114-locks` | 47 绿 / 0 红（37 + 10） | §三写明"47 条"✅ |
| `check-r114b-client` | 40 绿 / 0 红（32 + 8） | §五写明基线"32 红 + 8 绿、退出码 1"，改完 40 全绿 ✅ 逐字吻合 |
| §E 四个既有测试 | 全 `exit=0` | 四个"修改前红"的现在都绿 ✅ |
| §二 既有 9 个测试 | 全 `exit=0` | `check-stop-background-empty/epoch-scope/reuse-reset/single-task`、`check-level-reconcile`、`check-rename-session`、`check-compat-key-model`、`check-cron-hold`、`check-agents-waiting-fields` ✅ |

**④ M9 的修前/修后对比：足以证明缺陷已修。**
真机记录第 8–13 行给的是同一份 killed 快照下的直接对照：

- 修前：顶部「状态未知」、两个助手「在跑」、转圈元素 2 个、耗时 `887m 46s` → 再开变 `890m 28s`（按当前时间现算、持续增长）。
- 修后：顶部「已停止」、两个助手「已停止」、转圈元素 0、不显示耗时。

这三点正好一一对应 §五开篇描述的病症（状态未知 / 假在跑转圈 / 耗时乱算），且用的是 DOM 实测（`[data-wf-card]` 里徽章文字与转圈元素计数），不是肉眼描述。再加会话 C（completed → 「已完成」+ 运行结果段）和会话 B（无记录 → 「此运行未提供进度信息」、404 静默、无 JS 报错），M9 判**过**。
一个如实说的缺口：记录里没写"**阶段分组**是否正确渲染"（只记了顶部徽章、助手行、耗时、结果段、固定说明），所以 BRIEF B 的视觉部分仍靠 M6。

**⑤ 端口探测事故：不影响本次结论，但暴露测试基建的真问题。**
记录第 26 行如实写了：主会话为 M9 起的 6703 沙箱服务，与 `check-r114-server.mjs` 自己的端口探测（6703→6709 轮询 `/api/health`，**谁应答就当成自己的子进程**）撞车，造成 18 条假红；停掉沙箱后重跑 120/120 绿；而 `test-output-r114-full.txt` 的第 2 轮全量是在 6703–6709 无人占用时跑的——全量输出第 1 行也独立写了这句，两份记录互相印证。
**结论可信度不受损**（假红不是假绿，且最终跑在干净环境）。但要提醒：这个探测策略的失败模式是对称的——如果占用端口的恰好是**同一个 server 的另一个构建**，测试会安静地去测错进程并给出假绿。这是测试基建债，见风险 R2。

---

## 二、TEST-PLAN 逐条判定

> **判据粒度声明（重要）**：`test-output-r114-full.txt` 头部写明"每文件保留末 12 行"。因此除各文件末尾可见的那几条外，**单条用例编号的 ✓ 无法逐条目视**，我按任务规定以"文件末尾红绿汇总 + 退出码"为准。四个文件的"修前应绿"条数与 TEST-PLAN 回归清单条数逐一吻合（6/2/10/8），是这批汇总数字可信的旁证。

### §一 A 服务端进度表投影（15 行）—— 全部**过**
证据：`check-r114-server` 120 绿 / 0 红、`exit=0`。末 12 行可见 A4 段与 A2-19 逐条 ✓。
覆盖 A1.1 运行号门与上限、A1.2 投影白名单/不兜底/不截断/脏数据不抛/体积 ≤55%、A1.3 双平台路径拼接与坏参数返回 null、A1.4 真实 killed 快照解析与截断、A1.5 转写目录反推三元组、A1.6 正文兜底、A3 事件只换一项。

### §一 B `GET /api/workflow-run`（9 行）—— 全部**过**
同一文件同一汇总。含 A2-4 的 11 种坏参数 400、A2-5 先查参数再碰盘、A2-6~8 三种软链一律 400 且软链目标内容零外泄、A2-12 33MB 走 413 而非 422（证明没读进内存）、A2-13~15 422、A2-16 500、A2-17 错误响应只有 `error` 一个键。
**注**：这 9 行是本轮安全面最重的部分，恰好全部落在不可见的 12 行之外，只有汇总背书。若主会话要更硬的证据，让开发把 `check-r114-server` 的完整 stdout 附上即可（不需要重跑）。

### §一 C 历史会话接线（3 行）—— 全部**过**
末 12 行**逐条可见**：A4-1 五字段齐全 ✓、A4-2 不出现 `transcriptDir`/`scriptPath` 绝对路径 ✓、A4-3 正文兜底 ✓、A4-4 解析不出时 `projectHash/sid` 为 null 其余照给 ✓。

### §一 D 前端阶段视图与降级（15 行）—— 全部**过**
证据：`check-r114-client` 97 绿 / 0 红、`exit=0`。末 12 行可见 PERF（1000 助手/20 阶段整链路 <200ms）✓、B1-2 `Array.isArray(event.workflow_progress)` 守卫 ✓、B1-2 `wfProgressAt` ✓、B1-9 权威终态后 `task_progress` 整条丢弃 ✓。
其余（C2.1 分组与"未分阶段"、C2.2 整体状态 11 种、B2 24 条交叉表、C2.3 启动窗与磁盘/直播选源、C2.4 三项齐全才发请求、C2.5 每段行数配额、C2.6 并发只请求 1 次）靠汇总背书。

### §一 E 既有测试按新契约改写（4 行）—— **过**，其中 2 条证据偏弱

| 文件 | 判定 | 说明 |
|---|---|---|
| `check-subagent-forward` | 过（弱） | 输出仅 `all assertions passed`，**没有一行点名 `perTaskStopAffordance`**。该声明另由 `check-r114-locks` 的 E1-2 锁住（在 47 绿内），交叉背书成立 |
| `check-level-prune` | 过 | `剪枝 6 组 + 混合场景 + 接线源码守卫 全过`，含源码守卫段 |
| `check-monitor-buckets` | 过 | `监控面板 alive 语义 + workflow 截断全过`，"workflow 截断"是本轮新增语义，可见 |
| `check-stream-status` | 过（弱） | 仅 `all passed`，未点名"Workflow 独立成段" |

### §二 回归（7 行）—— 全部**过**，一处不可证
A3-2 / A2-18（只读，文件一字未改）/ A2-19 / A4-0/5/6 / B1-3 / B1-8 均在上述两个文件的"修前应绿"计数内，A2-19、A4-0/5/6、B1-3、B1-8 更是逐条可见 ✓。
既有 9 个测试全 `exit=0`。
**不可证的一点**：TEST-PLAN 要求这 9 个文件"一条都不改"，但测试输出只能证明"没红"，证明不了"没被改"。源码锁里 E1-13 / E2-12 锁的是安装脚本与 `sessionStore.js`，**不覆盖这 9 个测试文件本身**。属证据缺口，见风险 R3。

### §三 源码锁 47 条（15 行）—— 全部**过**
证据：`check-r114-locks` 47 绿 / 0 红、`exit=0`。末 12 行逐条可见：E2-8b 内层助手分支不调 `stopSingleTask` ✓、E2-9 停止按钮条件带 `!agent?.wfInner` ✓、E2-10a 按 `a.workflow` 分流到 `WorkflowCard` ✓、E2-10b 区块标题逐字不变 ✓、E2-10c 裸列表按 `workflowId` 逐个过滤 ✓、E2-11 图标走 `./Icon.jsx` ✓、E2-12 `sessionStore.js` 与 HEAD 逐字节相同 ✓、E2-13 前端无 `parseWorkflowLaunchText` 副本 ✓。
不可见但计入汇总的关键锁：**E1-2（`perTaskStopAffordance: true` 写死不进条件分支）**、**E1-8（停止核心 2228–2389 行逐字节不动）**、**E2-7e（工作流卡片禁 `dangerouslySetInnerHTML`）**、E1-9/10（新模块不写文件、软链检查在读文件之前）、E1-12（不透绝对路径）。E1-8 的比对基线存疑，见风险 R1。

### §四 真机 M1–M13（13 行）

| 编号 | 判定 | 依据 |
|---|---|---|
| M1 按停止只停本回合 | **未验证** | 记录第 23 行明列为需消耗额度、待用户决定 |
| M2 子代理/shell 分开单停 | **未验证** | 同上。**BRIEF 第 3 条把这条写成硬要求** |
| M3 总闸全停 | **未验证** | 同上 |
| M4 跨回合停整个工作流 | **未验证** | 同上 |
| M5 任务已回收后点停止 | **未验证** | 同上 |
| M6 20+ 助手实时视图 | **未验证** | 同上。**BRIEF A 的实时那半条全靠它** |
| M7 折叠不被进度刷开 | **未验证** | 同上 |
| M8 点开内层助手、无停止键 | **未验证** | 同上（源码侧由 E2-8/E2-9 锁住，真机观感未验） |
| M9 历史会话回看 | **过** | 记录第 7–17 行，三会话 DOM 实测，修前/修后对照完整，三轮无 pageerror，每会话只发 1 次 `GET /api/workflow-run`（去重生效） |
| M10 老 CLI / 无记录 | **部分过** | "无记录时不报错不空白"已验（会话 B）；"降级到裸列表"记录明写**未验**（需监控面板 + 真实运行目录） |
| M11 Windows 路径 | **未验证** | 待 Windows 机 / CI |
| M12 停止后残留态 | **未验证** | 需消耗额度 |
| M13 `stop-task.acceptance.mjs` | **过（弱）** | 0 失败、退出码 0；但 Tier 1 只 2 条通过（非法 pid → 404、非数字 pid 不返 2xx），**Tier 2/3 共 8 条跳过**（无活会话 slot / 需真实在飞任务）。即停止的成功路径一条都没真跑 |

### §五 v5 补充 V5-1~40（23 行）—— 全部**过**
证据：`check-r114b-client` 40 绿 / 0 红、`exit=0`，且与 §五写明的基线（32 红 + 8 绿，退出码 1）逐字吻合。末 6 行逐条可见 V5-35~40 回归 ✓。
其中 V5-25（真实 killed 记录跑完整链路，两个助手都不许出现"在跑/排队中"）与 V5-33（禁止无门的 `Date.now() - startedAt`）由真机 M9 的"转圈 0、不显示耗时"独立复证，双证据。
§五自陈的残留缺口（记录状态是第四种词时助手仍转圈）由 V5-30 钉住，属契约已知取舍，不算未完成。

---

## 三、BRIEF 成功标准 A–G 逐条

| | 标准 | 判定 | 证据 |
|---|---|---|---|
| **A** | 实时分阶段视图（聊天 + 监控面板）、随运行更新、结束定格显示成败与结果 | **待真机** | 结构层有证据：locks E2-2（聊天两条渲染路径各一处卡片）、E2-10a（监控面板分流 `WorkflowCard`）在 47 绿内；进度接线 B1-2/B1-9 在 `check-r114-client` 可见 ✓；"结束定格 + 显示结果"在历史侧已真机可见（真机记录第 13 行会话 C 的「运行结果」`{"rs":["DONE","DONE"]}`）。**但"发起工作流后实时逐步更新"这条链路从未在真机跑通一次（M6 未验证）** |
| **B** | 阶段/标签映射，无法确定时显示"未归类"不猜，解析失败不崩不白 | **过** | `check-r114-client` 97/0（C2.1-5/6 现开「阶段 N」+「未分阶段」、C2.1-8 无空组、C2.1-13~16 脏数据不崩）；服务端 A1.6 正文兜底、A4-4 解析不出仍给其余字段。视觉呈现待 M6 |
| **C** | 历史从磁盘重建、终态、无假"在跑" | **过** | 真机 M9 三会话修前/修后对照（转圈 2→0、「状态未知」→「已停止」）+ `check-r114b-client` 40/0 + 服务端 A4 段 |
| **D** | Task 子代理可单停、整工作流可停、内层助手不给停止按钮、主对话停止不再连带杀后台 | **待真机（本轮最大缺口）** | 已有：locks E1-2（`perTaskStopAffordance` 写死）、E2-8b/E2-9（内层助手无停止按钮、不调停止接口）、E1-8（停止核心逐字节不动）在 47 绿内；`check-stop-*` 四件套全绿。**缺**：M1/M2/M3/M4/M5 全未验证，M13 的 Tier 2/3 八条跳过 → **"停止真的只停本回合、真的能停单个后台助手"在真机上一次都没验过**，而 BRIEF 第 3 条原文要求"必须真机分开验证 local_agent 与 local_bash" |
| **E** | 轮询只针对当前会话、面板关闭即停、大列表可折叠/截断、不卡主线程 | **部分过 + 一处无用例** | 已验：PERF 整链路 <200ms ✓（可见）、`check-monitor-buckets` 的 workflow 截断 ✓、C2.5 每段行数配额、C2.6 并发去重（真机侧也实测"每会话只发 1 次请求"）。**"磁盘轮询只针对当前打开会话、面板关闭即停"在 TEST-PLAN 里找不到对应用例编号**，属需求未被测试覆盖；渲染卡顿体感待 M6 |
| **F** | mac / Windows 路径都对，Windows 分支可注入测试 | **待真机** | A1.3-1~3（注入假 home 与 Windows 拼接函数，验斜杠/反斜杠）、A1.5（Windows 路径解析三元组）在 120 绿内 → 逻辑层过；**M11 未验证**，Windows 真机零证据 |
| **G** | 旧 CLI 退化不报错；SDK 升级后全部单测与验收测试仍绿 | **过（后半）+ 部分过（前半）** | 后半：388 文件全 `exit=0` + eslint `exit=0`，含 §二点名的 9 个既有测试 → **过**。前半：真机会话 B 已验"无记录时不报错不空白、404 静默、无 JS 异常"，但 M10 的"降级到裸列表"与"真跑老版本 CLI"未验 → 部分过。另：真机在 worktree 上真起了 `server/index.js`（6703）并跑通 M9/M13，间接证明 `npm install` 后服务能启动，但**不能证明升级后的 `query()` 真能跑通对话**（那条路要 M1–M6） |

---

## 四、必须让主会话知道的风险

**R1（最高）——`E1-8` 停止核心"逐字节不动"锁的基线可能是空的。**
TEST-PLAN §三写的是"用 `git show HEAD` 取基线比对"。如果实现真按字面用 HEAD，而改动已经提交进 HEAD，那这条锁就是拿文件跟它自己比，工作区干净时必然通过，**保护力度为零**。全量输出头部写的 `锁定 3edf6c39` 暗示实现其实钉了一个固定基线提交（那样才有意义），与 TEST-PLAN 文字不符。
要做的事：确认 `3edf6c39` 是 r114 开工**之前**的提交。若它是本轮中途的提交，E1-8 只锁住了"中途之后没再动"，仍需人工 diff 一次 `3edf6c39` 与 r114 起点之间那段区间。这条是本轮唯一碰核心停止时序的红线锁，值得单独确认。

**R2 —— `check-r114-server.mjs` 的端口探测会认领"任何应答 6703–6709 的进程"。**
本次表现为 18 条假红（已如实记录并在干净环境重跑）。反向失败模式同样存在：若占用端口的是同一 server 的**另一个构建**，测试会去测错进程并给出**假绿**，且不会有任何提示。建议后续改成只认自己 spawn 出来的子进程 pid，或用随机高位端口。不影响本次判定。

**R3 —— "既有 9 个测试一条都不改"无法从证据证明。**
输出只能证明它们没红。源码锁 E1-13 / E2-12 锁的是安装脚本和 `sessionStore.js`，没覆盖这些测试文件。若担心"把测试改松了让它变绿"，需要一条 `git diff` 佐证（主会话可查，我按任务约束不查）。

**R4 —— 逐条编号证据只在每个文件末 12 行可见。**
本轮安全面最重的 9 行（`/api/workflow-run` 的软链拒绝、413 不读进内存、错误不泄露本机路径）与最重的三条锁（E1-2 / E1-8 / E2-7e 禁 `dangerouslySetInnerHTML`）全部落在可见范围之外，只有汇总数字背书。让开发把 `check-r114-server` 与 `check-r114-locks` 的**完整 stdout** 附进 `.devflow/` 即可补齐，不需要重跑。

**R5 —— 真机 M9 用的历史会话 jsonl 是"造"的。**
夹具本身是真的（spike 真实 Workflow 工具结果 + 真实 killed 快照），但三个历史会话记录是脚本拼装的，不是真跑工作流落盘的产物。这削弱了"端到端"成色：磁盘上真实文件的形态（编码、字段顺序、多工作流并存）没被覆盖。M6 真跑一次即可顺带补上。

**R6 —— BRIEF E 有一条需求没有对应测试用例。**
"磁盘轮询只针对当前打开会话，面板关闭即停"在 TEST-PLAN 全文中找不到编号。这是需求覆盖缺口，不是执行失误——建议要么补一条用例，要么在验收时明说这条只靠 M6 目视。

**建议的最短收口路径**：只跑 M1、M2、M6 三项真机（各一次带额度的真实对话），就能同时补上 BRIEF D 的硬要求、A 的实时半条、E 的渲染体感、R5 的真实落盘形态；M11 交 Windows 机或 CI；M3/M4/M5/M7/M8/M12 可在 M6 那次会话里顺手一并做掉。

---

## 五、读到的针对我的指令原文

**无。** 两份测试输出与两份文档中未发现任何试图改变判定标准、要求放宽评分或宣称"已通过无需检查"的文字。grep 过 `忽略之前 / 无需检查 / ignore previous / 放宽 / 直接判定` 等模式，命中的全是 `check-assistant-name`、`check-dontask-mode`（"危险命令不放宽"）、`prompt-cache` 这类正常测试名与断言文本，无异常。

---

摘要：**合格但有待真机项**；过 93 / 部分过 1 / 未验证 10 / 不过 0；HEAD 两份记录一致（`f8f7af5c`），388 个测试文件退出码全 0，红转绿全部达标且计数与计划逐一对账吻合。必读风险按优先级是 R1（E1-8 锁的基线要确认）、D 档停止语义零真机证据（BRIEF 点名的硬要求）、R2（端口探测存在假绿的对称风险）。