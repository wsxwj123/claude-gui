# r114 真机核对记录(主会话执行;只含零花费部分)
生成:2026-09-07 00:05
环境:worktree HEAD f8f7af5c(修前对照 = HEAD bac94646);client/dist 为对应 HEAD 的 vite build;服务 = worktree 的 server/index.js,HOME 指向自建沙箱(scratchpad/r114-real/home),端口 6703,NODE_ENV=test;不碰 6677。
数据:tests/fixtures/r114 的真实夹具 —— spike-wf-toolresult.json 的 Workflow 工具结果(taskId w1zi6gd0p / runId wf_631a4c46-1d3)+ snapshot-killed.json 快照;造了三个历史会话 jsonl(项目路径用主仓路径,只读)。
方法:无头 Chromium(playwright)从侧栏点进会话,读 [data-wf-card] 的 DOM(徽章文字、转圈元素数、助手行文字、有无停止按钮),并记录页面 JS 报错与 /api/workflow-run 请求。脚本 scratchpad/r114-real/{prep.mjs,m9.mjs};截图 m9-before-*.png / m9-after-*.png。

## M9 历史会话回看(TEST-PLAN §四 M9)
会话 A —— 被停止的工作流,磁盘快照 status=killed,两个助手在快照里 state=progress:
  修前:顶部徽章「状态未知」;两个助手徽章「在跑」,转圈元素 2 个;耗时显示 887m 46s,再次打开变 890m 28s(按当前时间现算、持续增长);无停止按钮。
  修后:顶部徽章「已停止」;两个助手「已停止」,转圈元素 0;不显示耗时;报错原文(Error: Workflow aborted …)以纯文本显示;底部固定说明「工作流内的单个助手无法单独停止;可停止整个工作流。」存在;无停止按钮。
会话 C —— 正常完成的工作流,快照 status=completed,助手 state=done 带 durationMs:
  修前:顶部「状态未知」;助手「完成」1m 21s。
  修后:顶部「已完成」;助手「完成」1m 21s;「运行结果」段显示 {"rs":["DONE","DONE"]}。
会话 B —— 有 Workflow 工具结果,但磁盘上没有任何记录(模拟老 CLI / 无快照):
  修前与修后一致:顶部「workflow 状态未知」,正文「此运行未提供进度信息」;快照请求返回 404 被静默处理(仅浏览器控制台一条资源 404 提示,无 JS 异常,页面正常)。
三个会话各只发起 1 次 GET /api/workflow-run(按 runId 去重生效);三轮均无 pageerror。
结论:M9 修后通过;M10 的"无记录时退化、不报错不空白"部分通过(其中"降级到裸列表"需监控面板 + 真实运行目录,本记录未验)。

## M13 tests/acceptance/stop-task.acceptance.mjs(BASE_URL=http://127.0.0.1:6703 打沙箱服务)
Tier 1:2 通过(非法 pid → 404;非数字 pid 不返回 2xx);Tier 2/3:8 跳过(无活会话 slot / 需真实在飞任务);0 失败;退出码 0。

## 未验证(需要真实调用 claude、消耗额度,等用户决定是否跑)
M1(按停止只停本回合)、M2(后台子代理与后台 shell 分开单停)、M3(总闸全停)、M4(跨回合停止整个工作流)、M5(任务已回收后点停止)、M6(20+ 助手真实工作流的实时视图)、M7(折叠不被进度刷开)、M8(点开内层助手看对话、无停止键)、M12(停止后残留态);M10 的"老版本 CLI 真跑"部分;M11(Windows 路径)待 Windows 机 / CI。

## 测试期间事故(如实)
主会话为 M9 起的 6703 沙箱服务,与 check-r114-server.mjs 自己的端口探测(按 6703→6709 轮询 /api/health,谁应答就当成自己的子进程)冲突,造成该文件 18 条假红;停掉沙箱服务后重跑 120/120 绿;.devflow/test-output-r114-full.txt 记录的第 2 轮全量是在没有任何沙箱服务占用 6703–6709 时跑的。
